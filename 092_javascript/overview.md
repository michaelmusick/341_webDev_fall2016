# Overview of JavaScript

variables

```javascript
var thisIsMyVariable = 0;

thisIsMyVariable = 32; // This is an integer
thisIsMyVariable = 3.2431; // This is a floating point or float
thisIsMyVariable = '^'; // char or character
thisIsMyVariable = "this is a string"; // A collection of characters

var arrayVariable = [0, 3, 8, 9];
arrayVariable = ["der", "hahahahaha", "blah", "9"];
                // 0        1           2      3

arrayVariable[2]

var myVar = 1;

myVar = myVar + 1; // 2
myVar = (myVar + myVar); // 4

// This is a Line Comment
/*
This is a block comment, and is the same syntax that CSS uses.
*/

function thisIsAnAdditionFunction(arg1, arg2) {
  var finalResult;
  finalResult = arg1 + arg2;
  return finalResult;
}

myVar = thisIsAnAdditionFunction(4, 3);

```

```c
int thisIsMyVariable

```
